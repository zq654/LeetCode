package LeetCode

func maxNumberOfAlloys(n int, k int, budget int, composition [][]int, stock []int, cost []int) int {
	//暴力
	res := 0
	for i := 0; i < k; i++ {
		curConsum := 0
		nowStock := make([]int, 0)
		count := 0
		copy(nowStock, stock)
		for j := 0; j < n; j++ {
			composition[i][j]
		}
	}
}
